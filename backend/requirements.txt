# Terra AI Backend — Production Dependencies for Render
#
# PHILOSOPHY: Only pin direct dependencies. Let pip resolve transitive deps.
# Over-pinning causes version conflicts between packages (supabase vs httpx etc.)
#
# Python version: 3.11.9 (set in .python-version)
# Platform: Linux x86-64 (Render)

# ── PyTorch CPU-only ──────────────────────────────────────────────────────────
# CPU build avoids downloading 2 GB of CUDA libraries (Render has no GPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu
torchvision==0.27.0+cpu

# ── Web framework ─────────────────────────────────────────────────────────────
Flask==3.1.3
flask-cors==6.0.2
flask-limiter==3.9.0
gunicorn==22.0.0
PyJWT>=2.8.0

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv==1.2.2

# ── Google AI / Gemini ────────────────────────────────────────────────────────
google-generativeai==0.8.6
google-genai==2.3.0
google-api-python-client==2.196.0
google-auth==2.52.0

# ── Supabase v2 ───────────────────────────────────────────────────────────────
# v2 supports httpx>=0.24.0 — compatible with google-genai's httpx>=0.28.1
supabase>=2.3.0

# ── HTTP ──────────────────────────────────────────────────────────────────────
requests>=2.28.0

# ── Geospatial ────────────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
shapely>=2.0.0
geopandas>=0.14.0
pyogrio>=0.7.0
pyproj>=3.5.0

# ── Image / plotting ──────────────────────────────────────────────────────────
pillow>=10.0.0
matplotlib>=3.7.0
# Headless OpenCV — server has no display, regular opencv-python will crash
opencv-python-headless>=4.8.0

# ── ML / Vision ───────────────────────────────────────────────────────────────
ultralytics==8.4.51
timm>=0.9.0
transformers>=4.35.0
tokenizers>=0.15.0
safetensors>=0.4.0
huggingface_hub>=0.20.0

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic>=2.5.0
tqdm>=4.65.0
PyYAML>=6.0
regex>=2023.0.0
rich>=13.0.0
typer>=0.9.0
psutil>=5.9.0
python-dateutil>=2.8.0
cryptography>=41.0.0
certifi>=2024.0.0