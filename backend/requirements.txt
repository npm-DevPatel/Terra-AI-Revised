# Terra AI Backend — Production Dependencies for Render
#
# PHILOSOPHY: Only pin direct dependencies. Let pip resolve transitive deps.
# Over-pinning causes version conflicts between packages (supabase vs httpx etc.)
#
# Python version: 3.11.9 (set in .python-version)
# Platform: Linux x86-64 (Render)

# ── Web framework ─────────────────────────────────────────────────────────────
Flask==3.1.3
flask-cors==6.0.2
flask-limiter==3.9.0
gunicorn==22.0.0
PyJWT>=2.8.0

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv==1.2.2

# ── Google AI / Gemini ───────────────────────────────────────────────────────
google-generativeai>=0.7.0
google-api-python-client==2.196.0
google-auth==2.52.0

# ── Supabase v2 ───────────────────────────────────────────────────────────────
# v2 supports httpx>=0.24.0 — compatible with google-genai's httpx>=0.28.1
supabase>=2.3.0

# ── HTTP ──────────────────────────────────────────────────────────────────────
requests>=2.28.0
resend>=2.0.0

# ── Geospatial ────────────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
shapely>=2.0.0
geopandas>=0.14.0
pyogrio>=0.7.0
pyproj>=3.5.0

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic>=2.5.0
tqdm>=4.65.0
PyYAML>=6.0
regex>=2023.0.0
rich>=13.0.0
typer>=0.9.0
psutil>=5.9.0
python-dateutil>=2.8.0
cryptography>=41.0.0
certifi>=2024.0.0